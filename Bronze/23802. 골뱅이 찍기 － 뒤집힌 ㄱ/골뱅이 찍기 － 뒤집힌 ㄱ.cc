#include <iostream>
using namespace std;
int main() {
	int count;
	cin >> count;
	for (int j = 0; j < count; j++) {
		for (int i = 0; i < count; i++) {
			cout << "@@@@@";
		}
		cout << "\n";
	}
	for (int j = 0; j < count; j++) {
		for (int i = 0; i < count; i++) {
			cout << "@";
		}
		cout << "\n";
	}
	for (int j = 0; j < count; j++) {
		for (int i = 0; i < count; i++) {
			cout << "@";
		}
		cout << "\n";
	}
	for (int j = 0; j < count; j++) {
		for (int i = 0; i < count; i++) {
			cout << "@";
		}
		cout << "\n";
	}
	for (int j = 0; j < count; j++) {
		for (int i = 0; i < count; i++) {
			cout << "@";
		}
		cout << "\n";
	}
}