#include <iostream>
using namespace std;
int main() {
	int count;
	cin >> count;
	for (int i = 0; i < count; i++) {
		cout << " @@@   @@@ ";
		if (i != count - 1) {
			cout << " ";
		}
	}
	cout << "\n";
	for (int i = 0; i < count; i++) {
		cout << "@   @ @   @";
		if (i != count - 1) {
			cout << " ";
		}
	}
	cout << "\n";
	for (int i = 0; i < count; i++) {
		cout << "@    @    @";
		if (i != count - 1) {
			cout << " ";
		}
	}
	cout << "\n";
	for (int i = 0; i < count; i++) {
		cout << "@         @";
		if (i != count - 1) {
			cout << " ";
		}
	}
	cout << "\n";
	for (int i = 0; i < count; i++) {
		cout << " @       @ ";
		if (i != count - 1) {
			cout << " ";
		}
	}
	cout << "\n";
	for (int i = 0; i < count; i++) {
		cout << "  @     @  ";
		if (i != count - 1) {
			cout << " ";
		}
	}
	cout << "\n";
	for (int i = 0; i < count; i++) {
		cout << "   @   @   ";
		if (i != count - 1) {
			cout << " ";
		}
	}
	cout << "\n";
	for (int i = 0; i < count; i++) {
		cout << "    @ @    ";
		if (i != count - 1) {
			cout << " ";
		}
	}
	cout << "\n";
	for (int i = 0; i < count; i++) {
		cout << "     @     ";
		if (i != count - 1) {
			cout << " ";
		}
	}
}